# 旧 Token 计费/欠费提案退役决议（2026-09-26 已批准）

本 change 的租户售价、每百万 Token 价格、费用、账单、支付、欠费、独立模型资格和向 EduPlus2 发限制/恢复请求，均与现行 OMS 供给/统一赠送或充值额度/服务级准入契约冲突，**禁止实施旧 tasks、同步旧 spec 或以 `--skip-specs` 归档**。

可复用的供应商采购金额与有证据成本观察迁入 `add-enterprise-oms-business-logic` 的 OMS-only 成本权限；不产生租户账单。用户已单独批准**退役旧目标**，旧 proposal/design/spec/tasks 保留历史，禁止继续实施。此次仅标记停用，**不归档、不同步冲突旧 spec、不提交、不触碰生产数据**；未来若需处理冲突 delta，须按 OpenSpec 管理流程另行审阅。
