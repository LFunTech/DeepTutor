> **退役目标已获用户批准（2026-09-26）**：以下任务仅作历史保留，全部禁止 apply、勾选、归档或同步冲突旧 spec；现行替代见 `add-enterprise-oms-business-logic`。

## 1. 契约与权限
- [ ] 1.1 核对真实 usage ledger 字段、幂等键和 EduPlus2 独立模型资格请求/回执/webhook 契约；无请求接口时明确人工处理降级，不标记自动限制模型完成。
- [ ] 1.2 定义价格/成本/欠费的细粒度 ops 权限、默认角色、菜单入口、审计字段及成本数据边界；覆盖 tenant_admin 伪造平台能力负例。

## 2. 持久化与核算
- [ ] 2.1 增加 PG 版本价格、成本、费用快照/调整、欠费/outbox 表及约束、索引、RLS 和迁移/回滚测试。
- [ ] 2.2 以真实 settled usage 计算 Decimal 费用，处理有效期、币种、历史不漂移、重试/冲正、待用量/待定价；编写边界测试。
- [ ] 2.3 独立计算供应商成本和拆分，缺单位/价格/汇率不猜测；编写 TMS/租户/普通报表成本泄露负例。

## 3. 欠费闭环与接口
- [ ] 3.1 实现 OMS 授权计费读写 API、后端 descriptor、分页/筛选和完整审计；前后端权限一致。
- [ ] 3.2 实现幂等欠费模型限制/恢复请求 outbox、EduPlus2 回执/webhook 确认与对账；只由权威模型资格事件改变模型准入，不改租户生命周期。
- [ ] 3.3 验证超时、拒绝、重放、并发、请求与事件乱序、外部系统不可用时的 pending/needs_attention 文案和人工路径。

## 4. 集成验收
- [ ] 4.1 用真实 provider usage 测租户→用户→调用费用汇总和导出一致性；分别覆盖有 usage、无 usage、中断、非 token 单位。
- [ ] 4.2 验证欠费下登录/exchange/TMS/OMS/历史/非模型操作可用、所有模型 dispatch 被拒及恢复；运行迁移、权限、隔离、精度与 EduPlus2 联调，记录残留限制并完成上游兼容审查与 OpenSpec 验证。
