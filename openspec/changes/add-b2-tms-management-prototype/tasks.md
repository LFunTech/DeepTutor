> 本 change 仅规划 B2 的 TMS 前端原型；A1/A2/A3 的本地/单租户运行基线、B1 EduPlus2 接入、C1/C2 OMS 生产能力及条件性 H 高可用分别由其实施 change 验收，本 change 不代替迁移或生产切换。

## 1. B2 规划与契约

- [x] 1.1 审计 DeepTutor 现有 `web/app/tms` 占位、`web/app/(admin)/admin`、企业 API/身份和旧企业文档，确认本地/云端产品边界及尚未实现的 TMS 能力。
- [x] 1.2 按用户“配额由 OMS 配置、TMS 只能查看”的决策重订 proposal、design、delta spec：配额一级只读列表、统一赠送/充值记录、只读详情与真实消耗明细、OMS 独占配额写入，以及六组列表→详情 IA。
- [x] 1.3 用户审阅本版 proposal、design、delta spec：确认独立前端部署、当前租户权限、配额只读边界、OCR 业务组件共享和原型非生产边界。
- [ ] 1.4 同步 `openspec/config.yaml`、`docs/enterprise/02-*`、`11-*`、`12-*` 及依赖的 B2/C1 实施提案，移除“复制/微调 DeepTutor Web 即交付 TMS”和“TMS 可写内部额度/上限”的旧误解；未批准的实施任务继续阻断。
- [x] 1.5 用户审阅本次 Skill owner 契约：云端仅 `global`/`tenant`，builtin 属只读 global 且云端默认未授权，OMS 对 global 按租户授权即用，TMS 不二次分配且可上传本租户 Skill；同名上传须管理员确认且 tenant 优先，个人 Skill 不迁移；确认共享列表及抽屉/模态框交互后再实施。
- [x] 1.6 用户审阅 Skill ZIP-only 修订契约：租户创建/更新仅提交完整 ZIP，Skill 内容元数据由 `SKILL.md` 唯一提供；owner/状态由可信平台确定，确认文件预览、错误提示、同名确认和取消保留文件的交互后再改原型。
- [x] 1.7 用户审阅本次写入 proposal/design/delta spec 的租户品牌契约：TMS 用可信 `school_code`（租户 code）和 EduPlus2 `tenant_id` 取得调色板，与 OMS 共用 token；原型受控演示标识和正式身份上下文分离，审阅通过后实施。
- [ ] 1.8 请用户审阅学校域术语与学校 code 路由修订：保留 TMS、`/tms` 和技术 `tenant.*`/Skill owner，产品名为“学校智能体管理后台”；一个管理员账号仅归属一所学校，`/tms/{schoolCode}` 只定位页面不授予权限，管理员身份 webhook 不与学校 lifecycle webhook 混同。

## 2. B2 独立 TMS 原型与共享组件

- [x] 2.1 按获批路径建立 TMS 独立构建/开发入口，验证其不依附 DeepTutor 本地 Web 或 OMS 应用构建；共享 `admin-ui`、`service-components`、契约包只作为版本受控依赖。
- [x] 2.2 在 TMS 原型消费与 OMS 完全相同的 OCR 服务列表/详情组件；OMS/TMS 各自使用独立 demo fixture 和动作适配器，检查共用列、筛选、分页、状态、导航、键盘与窄屏一致，禁止应用间 JSX 复制。
- [x] 2.3 实现工作台、成员与权限、应用与接入、服务与配额、知识与内容、用量与记录六组导航的对象列表→唯一详情；本租户服务列表与配额清单并列，Agent/工具/OCR 作为能力分类，筛选上下文与关联详情复用，不堆叠整站信息。
- [x] 2.4 用合成数据演示 EduPlus2 只读身份/租户状态、当前租户应用归口、单一配额清单中的赠送/充值筛选、只读详情、真实消耗明细、额度耗尽及 KB/文档状态；配额页面无任何写动作，其他允许的资源模拟写动作明确标注且不触达真实 API。
- [x] 2.5 在“服务与配额”维护 Skills 清单与详情抽屉，并与 OMS 共用安全 Skill 列表及 ZIP 预检/预览业务组件；将 tenant Skill 创建/编辑改为提交 ZIP 与同名新版本，全部内容元数据从包内 `SKILL.md` 解析，移除单文件/纯文本/手填元数据路径，Hub 需真实包或配对 fixture。保留 builtin 安全投影、待审核/发布、同名 global 独立确认（取消保留文件）、tenant 优先、无成员/应用二次分配；不伪称实时同步或真实安全审查。（Hub 地址仅作为未核验来源登记，真实拉取与审查仍属后续后端实现。）
- [x] 2.6 与 OMS 共用 Skill ZIP 分类错误：包结构/YAML 等普通格式问题与真正路径安全限制分开，展示条目、行列位置及修复建议；上传失败与再次保存均不吞掉具体错误，补 TMS 表单负例。
- [x] 2.7 在独立 TMS 原型消费共用 EduPlus2 品牌适配与管理 UI token，以受控部署配置提供配对的演示租户 code/ID，不将 fixture 内部 ID 或 URL 查询当成外部租户身份；有效租户色首屏生效，无可信上下文与接口失败均有明确兜底。
- [ ] 2.8 将 TMS 原型对外名称及学校业务文案改为“学校智能体管理后台”与“学校/学校管理员”，保留 TMS 工程缩写；将开发路由从 `/tms/prototype` 迁至 `/tms/prototype/{schoolCode}`，侧栏/抽屉深链/返回与品牌上下文均核对受控演示 code，旧入口迁移策略明确且生产所有原型路径仍为 HTTP 404。不把管理员账号 webhook 当作原型已交付功能。

## 3. B2 安全与边界验证

- [x] 3.1 验证 TMS DTO/API 模拟契约不含 OMS 平台 Secret、采购成本和跨租户明细；跨租户、未授权服务、他人私有资源、普通成员直达管理及任何配额/用量写入均拒绝；配额页面无写按钮，不把缺数据当零额度。
- [x] 3.2 验证开发 TMS 原型入口可评审、生产相同原型路径真实 HTTP 404，旧 DeepTutor `web/app/tms` 占位不被当云端生产入口；桌面/窄屏、加载/空/错误/同步延迟和前后端权限契约留测试证据。
- [ ] 3.3 完成后续真实 B2 接口/DB/OpenFGA/Keycloak 迁移判定与独立实施提案，明确配额写入仅属于 OMS，TMS 只有本租户配额/用量只读 API，并验证无 TMS 配额写路由；在真实身份、资源 owner/grant、EduPlus2 client、服务权益与调用用量 API 完成之前，正式 `/tms` 不从演示数据切换到生产。
- [x] 3.4 重新验证 Skill owner/来源和状态边界及 ZIP-only 格式负例：缺失/伪造 frontmatter、重复/危险/损坏/膨胀 ZIP、单文件/纯文本、包内自报 owner/状态、脚本未经审查执行均拒绝；普通成员、跨租户、未授权 global/builtin、内置/global 修改、缺 `requires`、同名确认取消、tenant 优先与撤销提示、跨端安全投影及用户级 API 旁路继续覆盖。（共享预检与双端交互负例通过；浏览器预检不代表服务端审查或运行时授权。）
- [ ] 3.5 在后续真实实施提案中定义 Skill `global`/`tenant` owner 的企业扩展存储、安全上传/审核发布、OMS 按租户授权即用、同名 tenant 优先且不可静默回退、运行时 `read_skill` 可信范围校验与本地个人 Skill 隔离；验证 HTTP/WS、CLI、SDK 等受影响入口并遵守核心改动审阅门禁。
- [ ] 3.6 验证 TMS 租户主题只由受控上下文选择：平台蓝/本租户紫配对、跨租户或伪造 URL 不能改色或扩大权限、无效颜色/网络失败回退、语义色与文字对比度、两端共享组件一致性和生产原型 404；浏览器视觉审计另留证据。（自动化正反例、两端构建与原有生产路由契约通过；浏览器视觉审计待补。）
- [ ] 3.7 验证学校 code 路由直达、刷新、前进/后退、抽屉关闭、筛选保留、学校 code 不匹配、资源越界、品牌不串校及开发/生产入口隔离；文档设计和合成 fixture 不得代替真实账号 webhook、登录与 API 403/404 验收。

## 4. C1/C2 与 H 协同门禁

- [ ] 4.1 ZIP-only 修订实施后与 OMS 原型共同运行共享组件包版本、两应用独立 build/typecheck/lint/交互与视觉回归；后续 C1/C2 服务目录和额度契约变更不得使已发布 TMS 静默失配。（共享版本锁定测试、两端构建/类型检查/lint 与自动化交互已通过；浏览器视觉回归待补。）
- [ ] 4.2 仅在后续真实 B2/C1/C2 实施完成时，验证 API 403/200、租户范围、登录/管理不断、单服务额度耗尽、审计关联与上游兼容 smoke；本原型文档校验不能代替生产验收。
- [ ] 4.3 条件性 H：如果 TMS 云端前端/后端启用多实例或发布要求高可用，按 G-H 验证共享会话、缓存/版本兼容、故障切换和回退；单实例须明确非 HA 与恢复目标。

## 5. OpenSpec 文档验证

- [x] 5.1 对本版重新运行 change-scoped 与全量 `openspec validate --strict`，检查跨 proposal 链接、旧计费/单体 Web/TMS 配额可写冲突及 `git diff --check`；用户批准及原型实现前，不勾选生产交付任务。
- [ ] 5.2 对本次增补的 Skill/新增契约重新运行两个 change 的严格校验、全量校验与文档一致性检查；实施后另运行两端构建、权限/交互回归和浏览器审计，不以文档校验代替原型验收。（两 change 与全量 26 项严格校验、两端构建/类型检查/lint、前端 100 项与运行时契约 3 项通过；浏览器审计待补。）
