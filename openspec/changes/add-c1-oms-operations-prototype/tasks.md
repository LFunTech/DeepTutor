## 1. 本 change：Agent 平台 OMS 规划重订

- [x] 1.1 对照当前 DeepTutor 设置导航、provider 编辑器、runtime registry、业务 router 和旧六个 proposal，记录旧目标与新决策的冲突；保留静态源码事实，不沿用旧计费/欠费结论。
- [x] 1.2 重写 proposal、design、delta spec、设置属性清单和资源作用域清单：OMS 平台管理、TMS 租户内部、EduPlus2 lifecycle、DeepTutor 运行核心、扩展优先与严格审阅门禁。
- [x] 1.3 固定列表→详情 IA、服务供给→统一租户额度（充值/赠送）→实际消耗、赠送优先、不同用量单位/待核对和无租户费用/欠费原则。
- [x] 1.4 按用户最新澄清重订产品边界与前端方案：DeepTutor 本地 Web 保留，云端 OMS/TMS 各自部署，共享设计系统及 OCR 等服务业务组件；单体 Web 旧假设标为过时。
- [x] 1.5 用户审阅并确认本版总体契约及 `add-b2-tms-management-prototype` 的共享组件边界；确认前不把旧原型或旧六个 proposal 当获批实现计划。
- [x] 1.6 用户审阅本次新增的五类目录新增草稿、Skill `global`/`tenant` owner、builtin 只读 global 且云端默认未授权、OMS 按租户授权即用、TMS 上传同名需确认且 tenant 优先、不二次分配及配对演示契约，审阅通过后实施相应原型任务。
- [x] 1.7 用户审阅 Skill ZIP-only 修订契约：OMS/TMS 创建和更新仅提交完整 ZIP；内容元数据只从包内 `SKILL.md` 读取，可信 owner/状态/授权不由包声明；确认包预检、安全审查和 Hub 待拉取边界后再改原型。
- [x] 1.8 用户审阅本次写入 proposal/design/delta spec 的 EduPlus2 品牌主题契约：OMS 无参平台主题、TMS 可信租户主题、环境域名配置、共享 token、完整天蓝色回退与原型/正式身份边界；审阅通过后实施。

## 2. 后续实施 proposal 重订（本 change 不直接实施）

- [ ] 2.1 收敛 EduPlus2 webhook proposal 到租户 lifecycle/对账；清理只为欠费准备的模型资格事件，验证登录/管理与配额不足的不同边界。
- [ ] 2.2 重订平台身份与 OMS 管理 API proposal：菜单/API/角色/默认授权闭环、目标租户隔离、管理写入审计、DB/OpenFGA/Keycloak 迁移判断及 403/200 正负例。
- [ ] 2.3 重订全服务 Provider 设置 proposal：全部设置管理归 OMS，保持 DeepTutor 字段/descriptor/执行语义，关闭旧企业管理入口；逐服务测试与生效确认。
- [ ] 2.4 重订用量总账 proposal：逐调用真实 Token 与非 Token 原生单位、Agent 底层子调用去重、待核对、幂等和服务消耗证据。
- [ ] 2.5 以独立“服务供给—租户权益/额度—实际消耗”proposal 取代旧 `add-oms-token-billing-and-arrears`；覆盖采购来源、统一额度列表、赠送优先、可授予量、并发预留/核对、耗尽准入、受控迁移和真实服务测试。旧 Token 售价、费用/欠费 tasks 不得实施。
- [ ] 2.6 重订操作界面 proposal：工作台、租户与权益、资源目录、资源供给、用量与运行、审计的对象列表→详情、全资源覆盖与权限/空/错/待核对状态。
- [ ] 2.7 同步 `docs/enterprise/02-*`、`11-*`、`12-*` 与旧 `/oms`、`/tms` 占位文案，明确单体 DeepTutor Web 前端、OMS 只读 Provider、按 Token 计费/欠费均已失效；未经实施 proposal 批准不改正式业务路径。

## 3. 高保真可复用前端原型（仅开发环境）

- [x] 3.1 建立独立 OMS 云端前端构建/开发预览入口和可由 TMS 消费的共享管理包；将旧 `web/features/oms-prototype/` 作为迁移/退役来源审查，不把 DeepTutor 本地 Web 作为生产 OMS 构建；`/oms` 在真实能力就绪前保持未启用。
- [x] 3.2 将 OMS 页面、共享业务组件和 demo fixture/适配器分开；提取 OCR 服务列表及至少一项非 LLM 服务列表为共享组件，统一共用列、搜索/筛选、分页、详情导航与状态；不让 TMS import OMS 应用代码。
- [x] 3.3 用管理后台结构替换旧卡片堆叠：六组导航、列表→详情→关联对象、表格筛选/返回保留上下文、窄屏与键盘可用；覆盖平台资源五类，不复制 `/settings` 整页。
- [x] 3.4 演示供给采购/补充、统一租户额度列表与获取方式、赠送优先的单次实际消耗、租户额度不足与平台供给不足、真实用量缺失待核对；无真实 API 写入或虚假财务金额。
- [x] 3.5 补菜单/操作权限显隐、加载/空/错误/无权限/待核对、OMS 开发路由与旧 DeepTutor Web 原型生产 404、敏感 DTO 隔离及费用/欠费旧 UI 不再出现的测试；浏览器按桌面和窄屏关键路径评审。
- [x] 3.6 与 TMS 独立构建验证同一共享服务组件的视觉/交互一致性、契约版本兼容及 TMS 不接收 OMS 专有字段；TMS 具体页面与租户场景由 `add-b2-tms-management-prototype` 验收。
- [x] 3.7 抽取可访问、窄屏可用的共享抽屉与确认模态框；OMS 列表→详情及深链改为列表上方逐级抽屉，关闭/返回保留筛选，不叠多层抽屉。
- [x] 3.8 补齐 OMS 平台资源五类、连接/服务配置、租户授权/额度及供给的可发现本地维护动作；按 DeepTutor 语义限制内置注册表身份和条件字段，草稿状态可回读，真实用量/审计/EduPlus2 lifecycle 保持只读。
- [ ] 3.9 增加平台管理员、运营、审计角色的动作显隐和二次确认/取消演示；补覆盖抽屉、各资源组维护、配额/供给约束、无网络写入及 TMS 只读边界的测试与浏览器审计。（角色与自动化测试已覆盖；最后一轮浏览器审计因本地监听 `EPERM` 待补。）
- [x] 3.10 将全部维护表单从列表/详情流中移入共享表单模态框；详情抽屉和筛选保持不变，复核取消恢复填写值；更新 OMS/TMS 共享组件回归、交互测试与设计文档。
- [x] 3.11 为五类平台资源目录及服务 Provider profile/模型、供应商资源方案/供给批次补齐权限受控的类型化新增：列表入口→独立表单模态框→本地草稿回读→详情抽屉；验证同作用域去重、必填/类型约束、取消不写入、未接入执行者不冒充已生效，以及内置注册表/租户/用量/审计不可新增。
- [x] 3.12 将 OMS global Skill 创建/更新改为 ZIP 包提交与同名新版本，`SKILL.md` 的全部内容元数据只读解析；通过共享包预检展示正文/资源清单、拒绝损坏或不合规包，移除纯文本/手填元数据路径；受控 Hub 仅在取得真实包或配对 fixture 后生成 Skill 草稿。保留 builtin 只读、审查发布、授权及后授权同名提示，与 TMS 共用安全组件且不调用用户级 `/api/skills`。（原型以操作员提供的 ZIP 与待核验 Hub 地址演示，不声称已由服务端拉取或核验来源。）
- [x] 3.13 将 Skill ZIP 错误分为包结构、`SKILL.md` 格式、包内容、文件类型、路径安全限制、数据损坏与浏览器兼容性等具体原因；显示涉及条目与修复建议，缺失 `SKILL.md`/YAML 错误不误称安全问题，预检失败后再次保存保留原提示；与 TMS 共用并补正反例测试。
- [x] 3.14 在独立 OMS 原型接入按环境配置的 EduPlus2 公开品牌响应：服务端无租户参数读取平台 `palette`、校验并首屏应用共享管理 UI token；缺失/异常时完整回退天蓝色，租户详情不改变 OMS 主题，不改 DeepTutor 核心或真实授权。
- [ ] 3.15 与 TMS 共测平台蓝/租户紫、环境域名切换、响应异常/无效颜色、对比度与首屏回退；两端分别运行测试、类型检查、lint、构建及桌面/窄屏视觉审计，生产原型 404 门禁保持不变。（主题自动化回归、两端类型检查/lint/构建已通过；本地浏览器视觉审计仍待补。）

## 4. 安全与验证门禁

- [ ] 4.1 对企业装配、旧 `/settings` 页面/API 和 CLI/SDK 可能的直连管理路径逐项审计；必要核心 seam 先单独提交严格审阅，不因原型直接改核心。
- [ ] 4.2 各实施 proposal 完成后分别验证并获得批准；DB/OpenFGA/Keycloak 仅在真实能力变更时走受控迁移；全入口身份、执行、session ownership、审计关联和 upstream 兼容 smoke 留证据。
- [ ] 4.4 在后续真实实施 proposal 中定义 Skill `global`/`tenant` owner 的企业扩展存储、受控导入/上传与审核发布、builtin 只读 global 默认未授权及包版本复核、OMS 按租户授权即用、同名 tenant 优先、运行时清单/`read_skill`/显式请求/`always` 全路径授权及本地个人 Skill 隔离；不得把原型的本地状态或现有用户级接口当作已具备的云端能力。
- [x] 4.3 对本次 ZIP-only 修订重新运行 change-scoped 与 `--all --strict`、检查文档冲突/尾随空白；原型实现后再运行 OMS/TMS 各自 typecheck、测试、lint、build、包格式/危险 ZIP/元数据来源/同名确认/权限负例与生产路由检查。不得把文档校验当真实 OMS 验收。（两 change 与全量 26 项通过；前端 100 项、两端构建/类型检查/lint、运行时契约 3 项通过。全局 `git diff --check` 仅被非本次范围的 PDF.js 许可证尾随空白阻断，本次范围检查通过；浏览器视觉审计仍在 3.9、3.15 待补。）
